services:
  docker-proxy:
    image: tecnativa/docker-socket-proxy
    container_name: docker-proxy
    environment:
      - CONTAINERS=1
      - IMAGES=1
      - NETWORKS=1
      - VOLUMES=1
      - POST=1
      - AUTH=1
      - INFO=1
      - EVENTS=1
    networks:
      - dockhand-internal
    privileged: True
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
  dockhand:
    image: fnsys/dockhand:latest
    container_name: dockhand
    volumes:
      - ${APPS_DATA_DIR}/dockhand/data:/app/data
    networks:
      - dockhand-internal
      - lefrigo-net
    ports:
      - '3000:3000'

networks:
  dockhand-internal:
    name: dockhand-internal
  lefrigo-net:
    driver: bridge
    name: lefrigo-net